{
  "name": "sscapital",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "wrangler pages deploy dist --project-name=sscapital"
  },
  "dependencies": {
    "astro": "^6.3.6",
    "nes.css": "^2.2.1"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
